' DesignTimeDbContextFactory is not needed when using Database.EnsureCreated()
' with SQLite. The EF Core migration tooling (dotnet ef migrations add) is not
' used in this project. Schema is managed entirely via EnsureCreated() on startup.
